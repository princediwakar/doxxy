-- Migration to add missing healthcare tables
-- Tables already exist in production, so this migration is a no-op

SELECT 'Healthcare tables migration - no changes needed as tables already exist in production' as status;